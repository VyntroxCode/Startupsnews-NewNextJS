import type { ReactNode } from 'react';
// Scoped Tailwind utilities for the Admin Rules page — see that file's header comment.
import '@/components/admin/rules-policy-tailwind.css';

export default function RulesPolicyLayout({ children }: { children: ReactNode }) {
  return children;
}
