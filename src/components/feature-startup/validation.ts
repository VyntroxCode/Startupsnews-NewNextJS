// Re-exports the shared lead-form validators (see src/components/lead-forms/shared/validation.ts) —
// the field set and rules are identical across all 3 lead-capture pages, only the page design differs.
export * from "@/components/lead-forms/shared/validation";
